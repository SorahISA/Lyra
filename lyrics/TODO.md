# TODO

- [ ] Make editor operations fully undoable with native `Ctrl+Z` behavior.
  - Include all formatting shortcuts (`Ctrl+B`, `Ctrl+U`, `Ctrl+L`).
  - Include structural edits that currently bypass native undo stacks.

- [ ] Reintroduce/improve beat support in a usable way.
  - Decide on beat editing UX (explicit beat fields vs inline markers).
  - Ensure comments/annotations align with beat positions.

- [ ] Add music integration and playback sync.
  - Embed sources (YouTube / Spotify where possible).
  - Support lyric-position tracking similar to online band score behavior.
  - Highlight current lyric line during playback.
